关于开发

`/client/`下的客户端工具类应当以HttpResult作为返回值，使用者通过判断返回值的code类型知晓响应结果，应该避免抛出异常，异常应当在调用过程中处理完毕

`/base_service/`应当大胆的抛出异常，并在异常中携带异常信息，供上层mapper调用过程中捕获进行处理

这种分层处理方式确保了：

- 客户端工具类的使用者可以通过 HttpResult 判断结果，逻辑简单且能避免反复处理异常。
- 基础服务层可以将底层异常暴露给上层，以便处理特定业务需求，并且不容易忽略重要的异常信息。